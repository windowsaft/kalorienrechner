{
  "tasks": {
    "dev": "deno run --unstable-kv --watch --allow-net --allow-read --allow-write --allow-env --env-file=.env main.ts",
    "build-docker-img": "docker build -t windowsaft/deno-app ."
  },
  "imports": {
    "@std/assert": "jsr:@std/assert@1"
  }
}
